# ============================================================
# Kindle Photo Frame — Master Config
# Edit this file to change all behaviour.
# ============================================================

# --- Google Photos shared album ---
album:
  url: "https://photos.app.goo.gl/yBPwxSGuEEnwnhGk9"
  pick_count: 15          # How many random photos per run

# --- Kindle Paperwhite 7th Gen output ---
kindle:
  width: 1072
  height: 1448
  dpi: 300
  output_format: "png"    # png = sharpest on e-ink

# --- Weather overlay (uses wttr.in — no API key needed) ---
weather:
  enabled: true
  location: "Ahmednagar, IN"  # "City, Country" e.g. "London, GB" or "New York, US"
  units: "metric"         # metric = C  |  imperial = F

# --- Date/time overlay ---
datetime:
  enabled: true
  format: "%d %A %b"              # e.g. "11 Wednesday Mar"
  timezone: "Asia/Kolkata"        # IANA timezone string

# --- Overlay bar appearance ---
overlay:
  position: "bottom"              # default, but script will now alternate
  background_opacity: 0.0         # 0.0 (clear) – no more black bar
  font_size_date: 80              # Larger for left side
  font_size_weather: 150          # GIGANTIC for right side (temp)
  font_size_desc: 40              # Small description below temp
  font_size_calendar: 28          # Small readable calendar grid
  font_size_sync: 32              # Subtle sync status
  padding: 60                     # Roomy padding

# --- Git output settings ---
git:
  output_branch: "processed-photos"
  commit_message: "chore: refresh Kindle photos [{timestamp}]"
  output_dir: "photos"

# --- Workflow schedule (informational — actual cron is in the workflow YAML) ---
schedule:
  cron: "0 * * * *"    # every hour
